# Backend for the AI Maturity Ladder app.
# The `dbtlabs` conda env already satisfies most of these; `fastapi` is the
# only addition. Pins are lower bounds — newer is fine.
fastapi>=0.110
uvicorn>=0.27
sse-starlette>=2.0
python-dotenv>=1.0
anthropic>=0.68            # Form Factors 1-3 (Messages API)
numpy>=1.26
fastembed>=0.8            # torch-free ONNX embeddings for RAG
oracledb>=3.0            # optional — RAG falls back to in-memory if absent/unreachable
claude-agent-sdk>=0.2     # Form Factors 4-5 (also needs the `claude` CLI on PATH)
